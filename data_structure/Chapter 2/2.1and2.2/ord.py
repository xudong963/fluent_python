# ord()是chr()[返回8位的ASCII码字符串]和unichr()[对于unicode对象]的配对函数
# 以一个字符作为参数,返回ASCII或者Unicode数值
# This is the inverse of chr() for 8-bit strings and of unichr() for unicode objects. 


print(ord('A'))
print(ord("A"))
print(chr(65))